const rodneCislo = Number(prompt('Zadej sve rodne cislo:'));

if (rodneCislo.length !== 10) {
    console.log("Uživatel zadal rodné číslo neplatné délky.");
} else {
    'Zadané rodné číslo má správně deset znaků.';
}